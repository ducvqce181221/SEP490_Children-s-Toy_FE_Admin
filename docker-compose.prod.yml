version: "3.8"

services:
  fe-admin:
    build: .
    container_name: toystore-fe-admin
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=5000
    ports:
      - "5000:5000"
    restart: unless-stopped
